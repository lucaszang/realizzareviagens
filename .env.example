# Places API (Places API precisa estar ativa no Google Cloud).
# Sem esta chave, a seção de depoimentos usa os textos estáticos do site.
GOOGLE_MAPS_API_KEY=

# Opcional. Se vazio, o site busca a ficha pelo nome da agência.
GOOGLE_PLACE_ID=
